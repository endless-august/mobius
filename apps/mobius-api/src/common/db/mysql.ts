// import mysql, { Pool, PoolOptions } from 'mysql2';
// import { DbMysqlConfig } from './index';

export class DbMysql {
    // pool: Pool;
    // constructor(config: DbMysqlConfig) {
    // this.pool = mysql.createPool(config);
    // }
}
